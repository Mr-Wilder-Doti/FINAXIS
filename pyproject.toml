[project]
name = "recommendation-robot"
version = "0.1.0"
description = "Base structure for recommendation robot"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q"
